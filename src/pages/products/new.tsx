import Layout from "@/components/Layout";
import React from "react";

type Props = {};

const NewProduct = (props: Props) => {
  return <Layout>NewProduct</Layout>;
};

export default NewProduct;
